# Testing

## Overview
This project uses both **white-box testing** and **black-box testing** to ensure the correctness and reliability of the Grade Calculator application.

All tests are implemented using **Jest** and are located in the `/test` directory.

---

## Running the Tests

To run all tests, use the following command:

```bash
npm test